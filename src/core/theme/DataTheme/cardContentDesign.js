import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  changePositive: {
    color: 'white !important',
    marginTop: '1rem',
    marginBottom: '0.3rem',
  },
}));

export { useStyles };
